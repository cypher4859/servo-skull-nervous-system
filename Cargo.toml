[package]
name = "servo-skull-nervous-system"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
bitflags = "2"
serde_with = "3"   # nice for millis/seconds helpers (optional)